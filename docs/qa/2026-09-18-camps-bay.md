# Camps Bay trainer experience — verification 2026-09-18

## Executed
- Unity 6000.6.2f1 C# compilation and persisted scene generation.
- Route / clearance / non-overlapping building / rig persistence assertions.
- Mobile URP full-lap runtime: 629.2 m, five camera checkpoints, no route/camera discontinuity at loop seam.
- Start without trainer rejected; started from menu after fresh editor fixture data.
- HUD speed/power/pulse shows fixture values; expired values display dashes.
- Expired speed pauses and stops distance; zero speed does not move; explicit pause cannot accumulate distance.
- QA feed cannot earn `TrainingMetres`; fixture excluded from exported IL2CPP player code.
- Original music clip loaded/playing; mute/volume change and restoration passed.
- Native macOS screen-control click on `Geräte & Einstellungen` opened the actual Unity Game-view settings panel.
- Rendered menu, devices, promenade, climb, descent and disconnected state visually reviewed. No external mockup substituted for runtime.
- Unity iOS export: `CapeCrown-20260918-173955`, zero errors, actual Xcode project and Info.plist inspected.
- Export includes native ObjC++ source with ARC, linked CoreBluetooth, German Bluetooth purpose text, `TARGETED_DEVICE_FAMILY = 2`, and landscape only.
- Unity's linker retained `OnBluetoothEvent`; `ReviewFeed` is absent from player code.
- Native ObjC++ syntax checked using installed macOS SDK. CI additionally checks iPhoneOS SDK (see Actions).

## Not verified / not implemented
- Not yet a signed Xcode application or physical iPad/KICKR/HR test. The Unity export is not an IPA.
- No measured iPad frame rate, thermal behavior or battery budget.
- Unity Click protocol adapter and acknowledged trainer resistance control are still pending. This bridge only reads telemetry.
- Route is Camps Bay-inspired, not a geographic reproduction. Cyclist remains a stylized prototype.
- Music functional checks are not a listening-quality acceptance on iPad speakers.

## Evidence
Local (ignored): `Logs/ExperienceReview/result.txt`, `menu.png`, `devices.png`, `ride-0.png` … `ride-4.png`, `disconnected.png`, `Logs/ipad-export.txt`.
Re-run: **Story Cycling > QA > Review Trainer Experience**.
